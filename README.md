# Deliveroo-Clone-ReactNative
Food delivery app built using React Native.
The project is mainly the UI, but it implements some basic functionallity, which you would expect in a food ordering application.
